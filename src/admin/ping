const token = await firebase.auth().currentUser.getIdToken();
const res = await fetch('http://localhost:3000/admin/ping', {
  headers: {
    Authorization: `Bearer ${token}`,
  },
});
const data = await res.json();
console.log(data); // → { ok: true, uid: ... } or 403
